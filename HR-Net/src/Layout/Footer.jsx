import './Footer.css'

/**
 * Composant représentant le pied de page de l'application.
 * Il affiche le nom de l'application ainsi qu'une brève description.
 * @returns {JSX.Element} Composant React représentant le pied de page.
 */
const Footer = () => {
    return (
        <footer>
            {/* Texte affichant le nom de l'application et une brève description */}
            <p>HR net | Wealth Health employee records management tool</p>
        </footer>
    );
};

export default Footer;
